sudo: required
services:
  - docker

before_install:
  - docker build -t agshin2004/aws-travis-ci -f Dockerfile.dev .

script:
  - docker run -e CI=true agshin2004/aws-travis-ci npm run test -- --coverage
